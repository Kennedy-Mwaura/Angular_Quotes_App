import { PopularDirective } from './popular.directive';

describe('PopularDirective', () => {
  it('should create an instance', () => {
    const directive = new PopularDirective();
    expect(directive).toBeTruthy();
  });
});
